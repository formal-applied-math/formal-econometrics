# GENERATED by tools/formalization_yaml.py — do not hand-edit.
# Mechanical fields: regenerate with `--write`. Editorial fields:
# edit tools/formalization_meta.toml, then regenerate.
# Per-statement informal→Lean map: docs/coverage.md + benchmarks/.
version: v0.3
project:
  name: formal-econometrics
  authors:
    - Raphael Coelho
  license: Apache-2.0
sources:
  - title: Standard econometric identification literature (potential outcomes, difference-in-differences)
    authors: []
    id: standard-causal-inference-literature
    type: canonical results
    license: ""
    author_contacted: n/a
    prior_work: ""
status:
  scope: 1 theorems in econometric identification, built on Mathlib; 1 delivery-ready (full + library-wrapper).
  sorry_count: 0
  sorry_in_definitions: 0
  axioms:
    - propext
    - Classical.choice
    - Quot.sound
  main_results:
    - declaration: Econometrics.att_eq_did_contrast
      file: Econometrics/Identification/DiD.lean
      sorry_count: 0
      axioms:
        - propext
        - Classical.choice
        - Quot.sound
      literature_dependencies:
        - statement: "Difference-in-differences identification: parallel trends implies the ATT equals the DiD contrast of observable conditional means"
          source: ""
automation:
  methods:
    - method: interactive human authoring
      models:
        - Claude Code (authoring assistant)
      framework: Lean 4 + Mathlib; VS Code / Lean LSP
      tool_setup: lake build; AxiomAudit + verification-ledger + values-review gates
      prompting_notes: author edits Econometrics/<Section>/*.lean directly
  spend_usd: "0"
  notes: no machine autoformalization is wired to this repo yet; when it is, the scout-not-author rule applies — the pipeline opens the PR, a human reviews and merges, so no machine proof enters the library unreviewed
fidelity:
  divergences: 0 reduced-core (a faithful but reduced formal core of the informal statement) and 0 library-wrappers (a thin restatement consuming a Mathlib/BrownianMotion lemma); 0 placeholders. The reduced-core entries are the honest divergences — each is itemised in docs/coverage.md.
review:
  status: continuous — an 8-lens values-review panel runs at each proof-content change (docs/values-review.md); cadence machine-enforced by test_values_review_is_current
  reviewers:
    - Raphael Coelho
  notes: the review is an upgrade engine (ranked backlog), not a pass/fail gate
alignment:
  namespace: Econometrics
  statements:
    - source: identification (Saporito, Stochastic Processes)
      lean: 1 statements
      module: benchmarks/identification.json
      status: full 1 · wrapper 0 · reduced 0 · placeholder 0
      note: per-statement map in docs/coverage.md
acknowledgements: ""
